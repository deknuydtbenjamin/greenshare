export type UserType = {
  id: number;
  username: string;
  email: string;
  password: string;
};
export type PayloadType = {
  username: string;
};

export type RoleType = {
  id: number;
  label: string;
};
